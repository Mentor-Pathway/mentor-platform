<div class="mt-16 p-12">
  <div class="border">
    <%= @pathway.title %>
    <%= @pathway.details %>
    <%= @pathway.difficulty %>
  </div>

  <div class="border">
    <% challenges = @pathway.challenges %>
    <% challenges.each do |challenge| %>
      <%= challenge.title %>
    <% end %>
  </div>
</div>
